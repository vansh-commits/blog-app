{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "hono": "^4.7.7",
    "prisma": "^6.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
